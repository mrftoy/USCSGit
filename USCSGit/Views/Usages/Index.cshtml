@model IEnumerable<USCS.Models.Usage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Usage1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Load_Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Usage1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Load_Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UsageID }) |
            @Html.ActionLink("Details", "Details", new { id=item.UsageID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UsageID })
        </td>
    </tr>
}

</table>
